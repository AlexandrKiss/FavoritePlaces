<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_map">

    <fragment
        android:id="@+id/navigation_map"
        android:name="dev.kisurin.favoriteplaces.ui.map.MapFragment"
        android:label="@string/title_map"
        tools:layout="@layout/fragment_map" />

    <fragment
        android:id="@+id/navigation_place_list"
        android:name="dev.kisurin.favoriteplaces.ui.placelist.PlaceListFragment"
        android:label="@string/title_place_list"
        tools:layout="@layout/fragment_place_list" />
</navigation>